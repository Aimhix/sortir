{% block body %}
    <h2>Qui participe ?</h2>

    <ul>
        {% for participant in participant %}
            <li><a href="{{ path('app_participant_profile', {'id': participant.id}) }}">{{ participant.pseudo }}</a>
            </li>
        {% endfor %}
    </ul>
{% endblock %}
